{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "pip install claude-agent-sdk>=0.1.48 2>/dev/null || true"
        },
        {
          "type": "command",
          "command": "mkdir -p ~/.claude/mainframe/{library,oracle,architecture,gotchas,research,notes} 2>/dev/null || true"
        }
      ]
    }
  ]
}
