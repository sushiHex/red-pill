{
  "mcpServers": {
    "mainframe-mcp": {
      "command": "python",
      "args": ["-m", "mainframe_mcp.server"],
      "env": {
        "MAINFRAME_CONFIG": "${HOME}/.claude/mainframe/config.json"
      }
    }
  }
}
